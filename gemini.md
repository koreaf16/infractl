# gemini.md — InfraCtl 프로젝트 규칙 (Gemini CLI)

## 필수 참조 문서

- **모든 작업 전** `docs/infractl-architecture.md`를 반드시 읽는다.
- **각 페이즈 작업 전** 해당 페이즈의 디자인 파일을 반드시 읽는다.
- **변경이 생기면** 관련 문서를 즉시 업데이트한다.

## 참고용 소스 (수정 금지)

- `claude_cli/src/` — Claude Code CLI의 원본 소스. **UI 설계 참고용**으로만 사용한다.
- 이 디렉토리의 파일은 **절대 수정하지 않는다**.
- **별도 언급이 없는 한 조회도 하지 않는다.**
- 모든 기능 구현 및 적용은 **Go 코드(`cmd/`, `internal/`)** 에서만 수행한다.

---

## 코딩 규칙

### 1. 원자적 모듈화

- 파일당 **300라인 제한** 준수.
- **단일 책임 원칙(SRP)** 적용 — 하나의 파일은 하나의 책임만 가진다.

### 2. File Header (필수 DocBlock)

모든 소스 파일 최상단에 아래 메타데이터 주석을 **반드시** 기재한다.

```go
// Package <package명>
// File: <파일명>
// Description: <이 파일이 하는 일 한 줄 요약>
// Responsibility: <단일 책임 명시>
```

### 3. 디렉토리 구조

- 기능별로 상세하게 분리한다.
- 디렉토리 깊이는 **2~3단계**씩 주어 완전히 분리한다.

---

## Go 코딩 규칙

### 4. 에러 처리

- 에러를 무시하는 `_ =` 패턴은 **절대 금지**.
- 모든 에러는 `fmt.Errorf("context: %w", err)` 로 wrap하여 상위로 전파한다.
- 에러 메시지는 소문자로 시작하고 마침표를 붙이지 않는다 (Go 표준).

### 5. context.Context 전파

- 모든 I/O 작업(SSH, DB, HTTP, 쉘 실행)의 첫 번째 파라미터는 반드시 `context.Context`.
- `context.Background()`는 최상위 진입점에서만 사용. 내부 함수에서 직접 생성 금지.

### 6. 인터페이스 설계

- `Executor`, `Connector` 등 핵심 추상은 반드시 **interface로 정의**, 구체 타입을 직접 참조하지 않는다.
- 인터페이스는 소비자(사용하는 쪽) 패키지에 정의한다 (Go 관용구).

---

## 보안 규칙

### 7. 크리덴셜 처리

- 비밀번호, SSH 키, API 키를 **소스 코드에 하드코딩 금지**.
- 크리덴셜은 반드시 암호화 레이어(`internal/crypto`)를 통과한 후 SQLite에 저장한다.

### 8. 명령어 / SQL Injection 방지

- `os/exec`로 외부 명령 실행 시 사용자 입력을 **절대 쉘 문자열에 직접 삽입 금지**.
- DB 쿼리는 반드시 **파라미터 바인딩** 사용.

---

## 로깅 규칙

### 9. Structured Logging

- 로그는 `slog` 사용. `fmt.Println` / `log.Printf` 로 로그 출력 금지.
- **크리덴셜, 비밀번호, API 키는 어떤 레벨에서도 로그 출력 금지**.

---

## 테스트 규칙

### 10. 실제 연결 테스트

- SSH, DB 연결은 **mock으로 대체하지 않는다**.
- 통합 테스트는 `//go:build integration` 태그를 붙이고 CI에서 별도 실행한다.

---

## 빌드 규칙

### 11. 바이너리 출력 경로

- 컴파일 시 반드시 **`bin/` 디렉토리**에 출력한다.
- 올바른 명령: `go build -o bin/infractl ./cmd/infractl/`
- 프로젝트 루트 또는 기타 경로에 바이너리를 직접 생성하지 않는다.
- 참고: Windows 환경에서는 `go build` 명령어 실행 시 출력 바이너리 이름이 `infractl.exe`가 됩니다.

---

## 커밋 규칙

### 12. Conventional Commits

```
<type>(<scope>): <요약>
```

| type | 용도 |
|------|------|
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `refactor` | 동작 변경 없는 구조 개선 |
| `docs` | 문서만 변경 |
| `test` | 테스트 추가/수정 |
| `chore` | 빌드, 의존성, 설정 변경 |

---

## 절대 금지 사항

- 크리덴셜 하드코딩
- 에러 무시 (`_ =`)
- 구체 타입 직접 참조 (인터페이스 없이)
- 사용자 입력을 쉘 문자열에 직접 삽입
- SQL 문자열 포맷팅으로 조합
- `fmt.Println` / `log.Printf`로 로그 출력
- 크리덴셜 로그 출력 (어떤 레벨이든)
- SSH/DB 연결을 mock으로 대체한 테스트
- `bin/` 외 경로에 바이너리 출력
